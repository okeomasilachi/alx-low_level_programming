#include "main.h"

/**
 * mul - multiplies 2 digits.
 * @a: viriable to be multiplies
 * @b: variable to be multiplied
 *
 * Return: 1 c is a number, 0 otherwise.
 */

int mul(int a, int b)
{
	int result = (a * b);

	return (result);
}

#include <stdio.h>

/**
 * main - Prints "with proper grammar, but the outcome is a piece of art,i"
 *
 * Return: (0) as final output
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
